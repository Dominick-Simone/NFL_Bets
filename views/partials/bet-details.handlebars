<div class="bets">
    {{#each this}}
    <div class="bet-info">
        <p>Bet added by {{user.name}} on {{format_date createdAt}}</p>
        <p>{{user.name}} picked {{bet_pick}} to win the game!</p>
    </div>
    {{/each}}
</div>